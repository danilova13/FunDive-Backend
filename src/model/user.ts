export type User = {
    email: string;
    firstName: string;
    id?: number;
    lastName: string;
    phone: string;
}

export type UserForm = {
    email: string;
    firstName: string;
    lastName: string;
    phone: string;
}
